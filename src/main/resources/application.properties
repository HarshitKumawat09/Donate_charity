# Server Configuration
server.port=8080
server.servlet.context-path=/project

# Database Configuration
# IMPORTANT: Update the password with your MySQL root password
spring.datasource.url=jdbc:mysql://localhost:3306/athenahacks
spring.datasource.username=root
spring.datasource.password=honeyk@09

# MySQL Driver Configuration
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JPA/Hibernate Configuration
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# WARNING: create-drop will reset database on each startup
# Use 'update' for development, 'validate' for production
spring.jpa.hibernate.ddl-auto=create-drop